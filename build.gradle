plugins {
    id("java")
}

repositories {
    mavenCentral()
}

dependencies {
    implementation(files("libs/BusinessBal.jar"))
    implementation(files("libs/Essentials.jar"))
    implementation(files("libs/Invisiman.jar"))
    implementation(files("libs/Landmarks.jar"))
    implementation(files("libs/Lockette.jar"))
    implementation(files("libs/OSAS.jar"))
    implementation(files("libs/PermissionsEx.jar"))
    implementation(files("libs/ScheduledDeath.jar"))
    implementation(files("libs/project-poseidon-1.1.8.jar"))
}
